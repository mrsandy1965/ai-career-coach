import React from "react";

const OutlookIcon = ({ className }) => {
  // Example: render a colored circle for outlook
  return <span className={`inline-block w-4 h-4 rounded-full ${className}`} />;
};

export default OutlookIcon;
